<p id="notice"></p>
<div class="container px-5 px-sm-0">
 <div class="row">
  <div class="col-md-5">
     <h1>ジャンル編集</h1>
         <!--<div class="form-body p-4">-->
            <%= form_with model: @genre, url: admin_genre_path(@genre), local:true do |f| %>
                <p><span>ジャンル名<%= f.text_field :name, class:"form-control", placeholder:"ジャンル名" %>
          　     <%= f.submit '変更を保存', class:"btn btn-lg btn-block btn-info mt-4" %></span>
          　     </p>
            <% end %>
  </div>
 </div>
</div>