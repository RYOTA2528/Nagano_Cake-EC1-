<main>
 <div class="container px-5 px-sm-0">
   <div class="row">
    <div class="col-md-3">
      <h2>商品画像</h2>
      <div class="profile-container">
       <%= attachment_image_tag current_admin, :image, :fill, 100, 100, format: 'jpeg', fallback: "img/no_image.jpg" %>
      </div>
    </div>
      <div class="col-md-8 offset-md-1">
        <p>
         <strong>商品名   </strong>
         <%= @item.name %>
        </p>
        <p>
         <strong>商品説明   </strong>
         <%= @item.introduction %>
        </p>
        <p>
         <strong>ジャンル   </strong>
         <%= @genre.name %>
        </p>
        <p>
         <strong>税込み価格<span>(税抜価格)円</span>   </strong>
         <%= @item.price %>
         <%= @item.add_tax_price.to_s(:delimited) %>
        </p>
        <p>
         <strong>販売ステータス   </strong>
         <!--販売ステータスのどう表記すればいいのかわからないため一旦保留-->
   </div>
 </div>
 </main>
